pipeline {
    agent any
    environment {
        subject = "Jenkins - PAC"
        batchno = "dvsbatch2"
    }
        stages {
            stage("My First Job with environments") {
                steps {
                script {
                    println "My subject is ${env.subject} Done"
                    println "My batchno is ${env.batchno}"

                }
            }
        }
}
}
