pipeline {
    agent any
    environment {
        subject = "Jenkins - PAC"
        batchno = "dvsbatch2"
    }
        stages {
            stage("My First Job with environments") {
                scripts {
                    println "My subject is ${env.subject}"
                    println "My batchno is ${env.batchno}"

                }
            }
        }
}
